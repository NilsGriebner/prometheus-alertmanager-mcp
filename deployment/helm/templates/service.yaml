apiVersion: v1
kind: Service
metadata:
  name: {{ include "alertmanager-mcp.fullname" . }}
  labels:
    {{- include "alertmanager-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alertmanager-mcp.selectorLabels" . | nindent 4 }}